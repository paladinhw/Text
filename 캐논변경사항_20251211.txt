20251211
- EPCPowerStartEvent 진행
- Condition 이 들어가 있는 조건. 설명 아래에 있음. 이해 필요.

condition or and 가 있을수 있다. 
$ 가 있으면 start with  
"Value" : "PCS-CS Daemon Exec$", 이것으로 시작하는 variable 은 start with 임.
repoadapter.hh 455줄

400020 이지만 PCS-CS Deamon Exec& 아니면 EPCPowerStartEvent  json을 안만들어도 됨. Event 자체도 무시해도됨. 
event 들어왔고 start condition 부합하지만 end condition 부합하지 않으면 end condition 들어올때까지 기다려야 한다. 
(안들어오면 시간이 지난후 없앨 예정이다.)

Condition 은 start end msgid 에만 있다.

ResetEndEvent 에 end or 조건이 있는 Event 이다. (추후 설명 필요)

	MsgCond<std::string> *m_s_cond1;
	MsgCond<std::string> *m_e_cond1;
	
	vector< shared_ptr<MsgCond<std::string>>> vm_s_cond1;
	vector<shared_ptr<MsgCond<std::string>>> vm_e_cond1;
	
- 
	vector 든 아니든 
	- EPCPowerStartEventRepoAdapter.hh 에 생성자에서 아래 코드
	
	RepoAdapter::addStartMsgCond(2, (MsgCondBase*)m_s_cond1); 로 repoadapter에 등록하기만 하면됨. 
	
	Json 에서 읽을 것이고 어디다 저장할까? 뭐 json 으로 저장해야지...
	
	xxxRepoAdapter에서는  생성자에서 addStartMsgCond 를 사용해서 등록함. 
	
	그러면 CommonRepoAdapter 에서 언제 addStartMsgCond를 쓸것인가?
	
	//CommonAdapter 에서는 아래 start end msgid 등록하는 것이 중요함. 기존 Adapter 에서는 RepoAdapter 생성자에서 진행 Common은 안됨. 
	(void LogCollector::initMessageIDHandlersJ2FromCommonRepoDatas( void ) 329줄 Repo Adapter 초기화 할때)
	
		scra->addStartMsgIdJ2(scrd->m_startMsgId);
		scra->addEndMsgIdJ2(scrd->m_endMsgId);
		for(auto m1 : scrd->m_msgIdSet) {
			scra->addMsgIdJ2(m1);
		}
		
	여기에 추가로 MsgCond 하면 될듯.
	
	initMessageIDHandlersJ2Json 은 CommonRepoDatas 을 초기화하는 곳 commonRepoGroup.initialize(); //Read Json make CommonRepoDatas 을 이용해 
	이곳에서 startcondition은 만들어 줘야 할듯.
	
	//condition 작업 CommonRepoGroup initialize 에서 218 line
	
	
20251212
- Condition 는 initMessageIDHandlersJ2FromCommonRepoDatas 에서 초기화 진행함. LogCollector.cc 부터 진행함. (EPCPowerStartEvent) 
   - Addstartmsg, AddEndmsg 추가하는 이후에 Condition 이 추가됨. 
   
  - 지켜볼 것이 #2_12 초기화 부분,  
  - 지켜볼 것이  
  - onmessage_default 에서 mapsize EPC에서는 값을 가짐 SOPJ2 CommonRepoAdapter onMessage: StartMessage  Detected Ename %s Message msgId=%d stmsgsize %d"
  
 20251215
 - Condition 은 초기에 결정되니까. Station 아래에 있으면 안될듯. ExecuteMessageHandler -> OnMessage 에서 무조건 부름.
 - WaferSeq Event 는 다 진행하고 JSON을 안만듬.
 - Mids 삭제 필요.
 - DisplayEvent 유무.
 - ID 만드는 곳은
	DB_WACStatusLocationData.cpp  Repository/app/storage
	DB_GetId
 - LogCollector 1268줄 에서 Event 필터 제거 모든 이벤트가 onmessage_default를 통하도록 코드 작성
 
20251216
- 내가 만든 m_msgIdSet 에는 start end 가 들어가지 않는다. 이때문에 m_msgIdHandlerJ2Json 만들때 registerMessageHandlerJ2Json 를 start end 에도 진행함. 그래서 Map 에 중복해서 들어감. 
- m_msgIdSet 은 onMessage_default 에서도 사용해서 isstartmsg, mid, isendmsg에서도 사용됨. 
- Event 레벨에서 Option 항목 만듬.
- LoadData 에러로 종료 내일 아침에 만들어야 함.

20251217
- 이벤트Data.cpp 의 ID 는     obj->ID_WACStatusLocationData_ = out.id; 이렇게 DB로 부터 받음.
- ID 는 objectId와 같음.

//회의 진행.
$USER_STATION_LIST 에서 읽어서 , Split 해서 갯수 파악
USER_STATION_LIST 이 없으면 무조건 1 common 으로 사용. 
CommonRepoDatas 의 jra 는 vector 써야할듯.

LoadData는 덮어쓰면 될듯. 

WACStatusLocationEvent 는 CTX-nnn 이 -1 값이 표시자체를 안함.
CTX-stage 는 무조건 값이 있어야 함. -1인 경우 0으로 (String)

20251218
- lotsequence 이벤트 추가~

20251219
- ErrorEvent 진행. Function과 Variable 둘다 있는 케이스 만들어야 함.
- Both type 빌드됨. 월요일 테스트해야함.
- *P, **P 처리도 추가해야 함.